# Archived decision choice protection

Finding3939860832 is confirmed: the options list offered a choice beneath an archived decision and the older choice RPC could change it. The list now suppresses that control. Append-only migration20260905073644 adds invoker triggers that guard choice/status writes, including direct table writes; choice validation locks the parent so concurrent archive and choice serialize. The existing RPC is not replaced, preserving compatibility with the newer dependency's no-op behavior. Lifecycle errors explain that the decision must be restored.

Verification: 398 unit tests, formatting, lint and type checks pass. The sandboxed build failed reading TypeScript's showConfig output; the production fixture build outside that sandbox passed. All12 archived-decision and Home-refresh browser cases passed across three configured profiles. SQL034 covers either household member, archive/restore, rejected choice/clear/status writes, unchanged values/version, and foreign-household denial. Local database execution remains unavailable (connection refused); SQL034 needs hosted execution. No migration was applied to production and no dependency was imported.
