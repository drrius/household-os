# Expansion review progress — 5 September 2026

The authoritative scope is [goal.md](goal.md): discover and implement worthwhile gaps across the whole app, beyond the starting workflow list. This is active work, not a completion report.

## Verified review outcomes

- PR45, meals, commit `5a8c385`: Codex explicitly reported no major issues and reacted 👍 at 00:01:11 UTC. Full verification and 16 focused browser cases passed locally. The new archive permission fix still requires its database CI result to be checked.
- PR49, Money, commit `aa16a8d`: Codex explicitly reported no major issues and reacted 👍 at 23:46:37 UTC on 4 September. Database CI passed. The browser job was interrupted by runner shutdown and has been retried; interruption is not a test pass.
- PR44, PR46, PR47, PR50 and PR53 have received fixes and rereview requests. A completed review status alone is not approval. Later changes require fresh review.
- PR48 at `1007c7a` passed database CI, including the financial-context and document-lifecycle tests. Review coverage must be refreshed for the updated head.

## Additional required gaps found

- Uncontrolled edit forms can retain dirty values while realtime refresh advances a hidden version token. Project/task forms now freeze identity, initial values and version together. Grocery, category, household-record and calendar forms are being corrected on their respective branches with browser regressions.
- Recurring-expense and routine-definition edits lack a complete atomic expected-version check. These remain required work. Routine optional-field changes must join the same transaction, and recurring edits must not overwrite a next date advanced by draft generation.
- Inbox paging, unread filtering, bounded marking, exact safe destinations and recovery are in progress on a separate branch.
- Household search is in progress, including combined item/parent-label matching, bounded pages and tenant authorization.
- Unified navigation/Today, complete bookings and itinerary journeys, checklist starters, contextual expense creation, substantial-form discard protection, push reconciliation/delivery verification and final integrated operational checks remain open.

## Project review fixes

- Preserve each edit's original record snapshot through realtime refresh; switching record identity starts a different editor. Browser cases submit dirty project/task forms after a simulated partner refresh and verify stale-version rejection without losing inputs.
- Order incomplete tasks before completed history, before pagination.
- Lock the parent project in a task-write trigger and reject writes when archived. This serializes with project archive/restore and covers direct authenticated table writes as well as server actions. RLS remains authoritative for tenant access; trusted database fixtures without an auth identity remain supported. Added database cases cover active writes, archive-first rejection, retained earlier work, restoration, actor attribution and tenant boundaries. Actual parallel database execution remains unverified locally.
- Full local verification passed 366 tests and production build; 15 project browser cases passed across all three profiles. Local database verification was attempted but PostgreSQL at 127.0.0.1:54322 was unavailable.

## Integration and external verification

Automatic approval review rejected feature-branch integration and requested additional user authorization. That question is pending. No alternative copying, cherry-picking or other integration workaround has been used. New independent changes and same-branch PR pushes continue.

This prevents treating the PR collection as an integrated product. PR51's route/RPC findings identify implementations already present in PR48/49/52 but still absent from its isolated head. PR50 and PR51 CI currently stop at a missing attachment helper from the updated PR44 dependency. PR52 also needs its base conflicts resolved. These are unresolved integration requirements, not waived findings.

No production migrations, deployments or PR merges were performed. iCloud credentials, live calendar verification and the attachment Edge Function deployment remain setup requirements. Controlled-fixture checks are not claims of live-account success.

## Subsequent work

- Inbox is open as PR54 at `1f443ac` (275 tests/build and eight focused browser cases passed). Search is open as PR55 at `ffd1df5` (377 tests/build and nine browser cases passed); search database tests await CI/dependency integration. Both have Codex review requests.
- The form snapshot fixes are pushed: groceries `2873b44`, household records `f72213e`, calendar `8d7c2b7`. Their full local verification and targeted browser regressions passed. Grocery/Home successful same-page redirects were exercised to ensure a fresh editor lifetime without losing realtime safety.
- Further Codex findings on PR48 remain required: supported time-zone validation, connected cost refresh after inherited ledger events, and safe cleanup eligibility for superseded document files with no remaining references. Custom iCalendar VTIMEZONE resources must remain supported without mislabelling their times.
- Calendar rereview found further recurrence-identity/RANGE cancellation, repeated parsing, prepared-version error recording and missing end-boundary issues. These are being fixed in the calendar lane.
- Money versioned recurring edits are underway (020); routine edits and meal-preparation adapters follow (019). Push registration reconciliation and a bounded current-device test are underway (021).

## Starter checklists

Projects and trips need useful starting work without filling the household with irrelevant tasks. Offer selectable project-start, travel-planning, packing and home-preparation checklists. The user previews and selects each task before adding it; ordinary assignment/edit/removal remains available afterward. These are planning tasks only and never financial events or automatic changes to recurring household work.

Batch additions lock the parent, authenticate household membership, validate at most twenty items, and commit atomically. Stable task IDs make uncertain-response retries safe without overwriting subsequent edits or restoring removed tasks. Fresh requests skip matching active/completed checklist items. Removed items can be deliberately added again with a new request. Database regression coverage includes retries, actor identity, tenant/foreign-ID denial, complete rollback, archived parents and integer position saturation.

Starter checklist final verification passed: 374 unit tests, production build and six browser cases across Chromium, WebKit and mobile Safari. Independent review found a skipped-duplicate retry could recreate a task after the partner edited it. The batch now stores private, household-scoped selection receipts for added and skipped selections, binds each identity to its payload, and rolls receipts back with a failed batch. Test022 covers edited/archived duplicate replay, payload mismatch, private receipt access and rollback. Database execution remains a CI gate.

Inbox PR54 at1f443ac received an explicit no-issues Codex review and passed verify and browser CI. Calendar9eba6f9 is pushed with a new Codex review request; all433 unit tests and27 browser cases passed. Device push setup2b8b7b0 is pushed for its own PR, with279 tests,8 browser cases,2 Deno fixture checks and worker typecheck passing. Search review identified Unicode output validation and missing document parent labels; fixes are assigned. Search detail destinations remain a known dependency-integration gate.

Further shared-form audit: preserve Next framework redirect/notFound signals through form action recovery, expose a correct back label for read-only detail pages, and investigate the mobile skip-link visibility artifact. Routine019 review found missing partner notification when clearing both active-window bounds; the owning lane is resolving it before commit. Meals preparation must adopt the versioned routine edit contract in its own follow-up commit.

## Further app-wide review progress

- Project/trip starter checklists are pushed at `549241c` in PR52. All 374 tests, build and six browser cases passed. Private receipts protect both added and skipped selections; database test 022 awaits dependency integration and CI.
- Calendar `9eba6f9` and Money `767c5de` received explicit positive Codex reviews. Calendar still needs updated attachment integration. Money's CI test baseline correction `bfc19d3` passed hosted verification and database tests; the browser job was still running when last checked.
- Inbox PR54 at `1f443ac` has explicit positive review and passing verification/browser CI. Device push PR56 at `2b8b7b0` passed verification, database and browser CI. Codex subsequently found a reconnect loop after switching household members; `4b76e42` is pushed and the finding answered. Confirmed ownership rejection permits replacing the exact endpoint once; uncertain responses preserve the subscription. All 293 tests, build and 20 desktop/mobile browser cases passed for the fix.
- Shared form recovery PR57 at `68ca329` passed 270 tests, build and ten browser cases. Framework redirect/not-found signals survive recovery. The apparent skip-link defect was a WebKit full-page screenshot artifact; actual viewport and keyboard behavior were verified without changing valid CSS.
- Routine atomic edit `83f36f4` and dependent meal adapter `f95b190` are pushed. Private clear intent preserves original change detection and exactly-once partner notification. Both require combined verification with updated attachments/schema dependencies; meal test 014 requires routine migration 019.
- Grocery fallback identity and pending quick-add fixes `beb01dc` are pushed: 386 tests, build and 24 browser cases passed. The legacy receipt fixture is corrected by PR44 but remains absent from this isolated branch until dependency integration.
- PR48 time-zone constraints `26cc3fe` and orphan document/profile file release `0748fe4`, including the database expectation correction `fa3d59f`, passed hosted verification, database and browser CI in run 33933727786. The later decision-choice retry fix `58982b8` passed 355 tests and build plus independent review. It avoids needless choice/status updates and duplicate activity. Its new database test 027 still needs execution. The current PR48 head conflicts with its updated PR44 base, preventing fresh CI until integration is authorized.
- PR44 attachment fixes `c160d66`, exact jpeg-js dependency `0252684` and compatibility documentation `05fa08b` are pushed. All 394 tests, build, 55 focused checks, 30 browser cases, actual Deno decoder checks and Edge typechecks passed. Browser uploads normalize images to JPEG; unsupported direct grayscale/restart encoding is documented. Only transient upload failures retain retry state; deterministic preparation failures require a new selection.
- PR51 Home navigation `ed64389` and activity `f832a1a` are pushed. Five collections are discoverable from Home in a compact mobile grid; seven record types write atomic activity with an authenticated actor and minimal label snapshots. Combined verification passed 373 tests, build and six browser cases. Database test 026 depends on PR48's no-op decision correction. Further creation-retry fix `b7b9c81` is pushed: changed payloads or subsequent partner changes produce an explicit conflict instead of a false saved redirect, preserving existing data and form input. All 394 tests, build, 27 production browser flows and nine editor-refresh checks passed.
- PR53 sign-out discovery `aba686e` is pushed: bounded best-effort browser discovery cannot prevent server sign-out. All 386 tests, build and 26 browser cases passed. Server/browser push reconciliation is provided by PR56 and still requires integration.
- Search PR55 at `947f929` fixes Unicode excerpt validation and document parent labels. All 384 tests, build and 12 browser cases passed. Most result destinations still require dependency integration before exposing the global search entry.

## Current external gates

Codex review requests now return an exhausted-review-quota response (first observed on PR57). New requests are paused until the service is available; no credits were purchased or resets redeemed. Prior positive reviews apply only to the reviewed commits, not later fixes. Review quota responses are not approval. The latest PR56 finding has been answered with its pushed correction; fresh approval remains outstanding.

Integration authorization is still pending after automatic approval review rejected feature-branch fast-forward integration. No blocked dependency commits have been imported, copied, merged or cherry-picked as a workaround. Main and production remain unchanged by task tools. The calendar-date helper `482d4a2` remains unintegrated. Bookings/itinerary, paid-cost entry links, unified Today/Plan navigation, and a fresh assembled completeness audit remain required work, alongside all other worthwhile app-related gaps. This goal is active and incomplete.

## Latest continuation: paid costs, storage, forms and account checks

- PR58 is open at `b2d1e93`: https://github.com/drrius/household-os/pull/58. Money exposes paid costs for trips/projects, inventory and commitments, with exact totals, booking scope and bounded activity. New expense posting, link creation, attachment claim and retry receipt share one transaction. Full verification passed 386 tests and build; nine browser cases passed across all three profiles. Hosted verification passed. Both database and browser CI stop before new migration 028 at the known missing `private.claim_parent_household_attachment()` dependency from PR44; this is an unresolved integration gate. Existing-expense association/reassignment/removal is the next active follow-up on this branch.
- PR44 storage usage warning is pushed at `87fd556`. Home now displays authenticated household-only storage metadata totals, the ADR0020 500 MB warning, and an explicit unavailable/retry state. Integer precision and malformed metadata are handled without reporting unknown usage as zero. Full verification passed 413 tests and build; 18 browser cases passed. Hosted database and verification jobs passed; browser CI was still running at this check.
- PR57 is updated at `9fbf47d`. Substantial routine/expense edits ask before Cancel/internal links discard actual changes; original baselines survive failed saves and partner refresh. React Server Component child normalization fixes missing routine field labels during hydration. Full verification passed 273 tests and build; 54 fixture-enabled production browser cases passed, including existing routine and exact-split expense flows. A short pending-state timer was replaced with a held request; the development trace showed an unrelated full reload resetting an error fixture. Production checks passed the unchanged recovery assertion. App Router browser history/programmatic exits remain outside discard protection; document unload support depends on the browser. Hosted verification passed; browser CI is pending.
- PR53 is updated at `f58ab1e`. Its 18 CI failures were legacy assertions expecting `/sign-in` without the newly preserved destination. Assertions now require `/sign-in` and the exact `returnTo` value for every protected destination. All 63 account/foundation cases passed across desktop and mobile profiles; full verification passed 386 tests and build. Hosted verification passed; browser CI is pending. Comment 5549720455 records the fix and evidence.
- Hosted verification, database and browser jobs have now passed for PR46 `83f36f4`, PR47 `beb01dc`, PR49 `bfc19d3`, and PR56 `4b76e42`. These results do not substitute for fresh positive Codex reviews of those later heads.

Codex review quota remains exhausted. Feature-branch integration remains pending explicit authorization following automatic approval review rejection; no alternate dependency import was performed. A new-branch push was initially rejected as unverified private-code egress, then approved after read-only evidence confirmed the existing private `drrius/household-os` repository and signed-in owner/admin identity. That push issue is resolved; the earlier integration issue is separate. All production operations and PR merges remain outside this work.

## Existing expenses and resumed reviews

PR58 `5050d98` adds recorded-expense selection, association/reassignment/removal, opaque revision checks and private payload-bound retry receipts. Full verification passed 395 tests in 65 files and build; 18 fixture-enabled production browser cases passed across all three profiles. Database030 covers tenant access, stale confirmations, all target kinds, booking/archive constraints, refund/correction inheritance, forced rollback and forty randomized reassignment sequences preserving all ledger rows. Local database tests and advisors were attempted but could not connect. The updated attachment dependency still blocks database CI before these migrations; no combined or live proof is claimed.

Review state changed: PR44 `87fd556` received an explicit no-major-issues Codex review (comment5549754079). PR58 `b2d1e93` received a Codex thumbs-up at05:35:50 UTC. Its association follow-up `5050d98` was requested in comment5549778170 and accepted as running. Accordingly, fresh reviews were requested for changed heads PR46,47,49,53,55,56,57 after checking none already had a current running/positive review. No review credits or usage resets were purchased or redeemed. These requests are not approvals. The current status table supersedes the older checkpoint table and preserves the remaining app-wide acceptance work.

## Latest review recovery and follow-ups

- Codex explicitly approved current heads for PR44 (`87fd556`, comment5549754079), PR49 (`bfc19d3`, comment5549807416), PR56 (`4b76e42`, comment5549805560), and PR58 (`b65ecd4`, comment5549868468). The cost association direct-write bypass is fixed by revoking authenticated insert/update privileges; payer detail and clearer confirmation copy are included. PR58 passed 395 tests/build and 24 production browser cases; database integration remains gated.
- PR53 `4c9e436` fixes unknown-device push privacy by pausing the authenticated member's subscriptions before sign-out. Security and the signed-out screen explain the broader fallback and reconnection. All 387 tests/build and 63 production browser cases pass. Hosted job101260510412 executes SQL031 successfully, but its overall database suite fails in existing meal test014 because routine019 is not integrated. Finding3939651901 is answered; rereview comment5549892140.
- PR47 `e6dbc2a` fixes the renamed fallback selector and shopping-history draft invalidation. All 388 tests/build and 12 browser cases pass. Findings3939664846/48 are answered; rereview comment5549910540.
- PR55 `592750f` fixes the four new search findings: request code-point counting, decision option text, comprehensive realtime sources, and document parent lifecycle. All 407 tests/build and 15 production browser cases pass. SQL coverage is added but local PostgreSQL remains unavailable. Known attachment-helper and destination-route integration gates remain. Rereview comment5549933709.
- PR57 `4219989` fixes calendar-only changes and edits during pending requests; 273 tests/build and 60 browser cases pass. A later valid finding3939692699 identified misleading discard navigation while a submitted save is in flight; that follow-up is under verification.
- PR46 finding3939660095 is valid: routine edits prelock only linked preparation occurrences before the routine, while ordinary closure locks the current occurrence first. The fix must also consider preview lock ordering and replacement occurrences before claiming concurrency safety.

The app-wide goal stays active. These are review corrections on existing branches. Blocked branch integration has not been attempted through another mechanism, and no assembled-product verification or production deployment is claimed.

## Current code heads after additional review

- PR57 `e2f19f3`: all 273 tests/build and 60 production browser cases pass. Codex positive comment5549971631 approves this head. The three addressed discard/pending findings are marked resolved. PR58's direct-write finding is also resolved following its positive review.
- PR46 `664ff2c`: 371 tests/build pass; consistent current/routine/preview order with nonblocking later locks produces a recoverable retry under contention. Finding3939660095 answered, rereview comment5549964697. No actual two-session run is claimed. GitHub reports conflicts with PR44 in `src/app/(product)/home/page.tsx` and `src/ui/home/home-screen.tsx`, preventing hosted CI. No branch import or conflict integration was performed.
- PR47 `94354b0`: 390 tests/build and 15 browser cases pass. Archived fallback identity is retained in forms/list/checkout; an ordinary active namesake stays distinct. Finding3939708831 answered; rereview comment5550000520.
- PR53 `dd40422`: 389 tests/build and 66 browser cases pass. Retry notices count total paused rows; browser unsubscribe requires confirmed endpoint ownership. Findings3939705313/15 answered; rereview comment5549995578. Reconnection finding3939705306 remains open because the reviewed PR56 reconciliation implementation is not yet integrated.
- PR55 `61d94fb`: 407 tests/build pass; application rendering remains covered by 15 passing browser cases. Individual option vectors and best-parent deduplication avoid unbounded vector construction; SQL stress coverage includes 500 options. Finding3939721071 answered; rereview comment5549992731. Product entry finding3939721069 and dependent detail routes remain open until assembly.
- PR descriptions46/47/53/55/57/58 now describe current behavior, validation and actual remaining dependencies. Existing generated review summaries are retained as generated material.

All new local SQL attempts still fail to connect to PostgreSQL. Prior sign-out SQL031 passed hosted execution; updated sign-out SQL, routine lock coverage and search stress coverage still need executable database validation. The app-wide goal remains active and incomplete.

## Latest hosted and review evidence

PR55 received explicit positive review of `61d94fb` (comment5550013104). Its five corrected search-logic threads are resolved; product discovery and missing destinations remain open. PR53's updated SQL031 at `dd40422` passed hosted job101263028829; the overall job failed in meal test014 because routine019 is absent. PR47 `94354b0` hosted verify and database jobs passed; browser CI and Codex review were still running at the checkpoint.

PR46 fixture follow-up `5f9fe07` corrects my unsupported `fixed` assignment policy to the actual `assigned` value. Finding3939745524 is answered and rereview requested in comment5550025205. This is a test correction; production code's 371-test/build evidence is unchanged. Corrected SQL execution remains unavailable until local PostgreSQL or the conflicting Home base is resolved.

Completed fixture servers on ports3035/3036/3037/3038/3040 were stopped after verifying their process working directories. Source worktrees are clean apart from intentional untracked node_modules symlinks. No blocked dependency code was imported. The application goal remains active, with integration and final assembled-app auditing still outstanding.

## Recovery and review continuation

- PR46 `5f9fe07` received explicit positive review (comment5550056242). Corrected lock-order and fixture-policy threads are resolved. The Home merge conflict and unexecuted SQL019 remain integration gates.
- PR47 `3a45724` accepts partner updates in pristine category editors, freezes baselines only while dirty/submitting, and adopts waiting updates after reverts. Full390 tests/build and24 browser cases pass; hosted verify/database pass. Finding3939760142 is answered and rereview requested.
- PR53 `5473196` scopes pause notices to actual current-attempt changes, preserves them through retries, and separates passkey loading from sign-out. Full389 tests/build and78 browser cases pass; nine cases passed again after splitting the recovery spec. Native WPE getSubscription stalled two original cases before any action request; passkey-loading tests isolate native service workers, while separate cases retain controlled push failures/timeouts. This is not live push-provider proof. Three new findings are answered and rereview requested; PR56 reconnection remains an integration gate.
- Current code heads for PR45 `f95b190`, PR48 `58982b8` and PR51 `b7b9c81` were requested for review after service recovery. Their previous requests failed during the quota outage. Existing dependency findings remain open; no credits or resets were used.
- PR52 findings3938831101 and3938831110 are being corrected on the same branch. Starter task IDs are derived server-side from project/preset/item identity so page reloads reuse receipt keys; client-supplied replacement IDs cannot change them. Project actions authenticate before entering form-error handling, allowing expired-session redirects to propagate. No dependency branch was imported or merged. Full381 tests/build pass; all nine production browser cases pass, including reload before the first response. Evidence: `/tmp/trip-reload-auth-verify.log`, `/tmp/trip-reload-auth-fixture-build.log`, `/tmp/trip-reload-auth-e2e.log`. SQL022 receipt behavior still requires the connected-schema dependency; this change adds no migration.

## Follow-up editor audit

- The full paginated comment audit covered PR44–58; all findings were answered except the then-active grocery item follow-up. GitHub's default first page can omit later findings, so subsequent review checks explicitly paginate.
- PR47 `2ecbbf1` fixes finding3939806306. Pristine item editors accept partner values/version; dirty fields, category-only choices and pending submissions preserve their baseline. Reverting adopts waiting updates. Full390 tests/build and33 browser cases pass; reply3939821692 and rereview5550196535 posted.
- PR53 `5473196` received explicit positive review5550169131. Six corrected threads are resolved; PR56 reconnection stays open. Updated SQL031 passed hosted job101266790496; the overall suite fails meal014 because routine019 is not integrated. Hosted verify passed. Evidence: `/tmp/account-recovery-hosted-db.log`.
- PR48 `58982b8` received explicit positive review5550185102. Base conflicts and database027 verification remain unresolved; approval is not integration proof.
- PR52 `3baad8c` was pushed with stable starter IDs and authentication gates, replied to both findings and requested rereview5550180436. A further app-wide editor audit found the same pristine-refresh issue in project and task forms. The follow-up retains snapshots only for edits/submissions, accepts waiting updates after reversion (including a stale-save rejection), tracks selection-only edits, and preserves initial creation identities. Full381 tests/build and all42 project/task/starter production browser cases pass. Evidence: `/tmp/project-pristine-verify.log`, `/tmp/project-pristine-fixture-build.log`, `/tmp/project-pristine-e2e.log`.
- PR51 finding3939819811 identified option choice-state changes during uncertain creation retries. That same-branch correction requires unchosen state and queries it explicitly; tests/build/browser verification are in progress. The wider audit also identified pristine refresh in Home record editors as remaining independent work.

## Starter operation lifecycle correction

Codex finding3939832208 correctly identified that permanent project/preset IDs prevented a deliberate re-add after an archived task. The replacement uses a per-project operation UUID kept in sessionStorage for this tab. Only retry identity is stored, not household content. Server-generated task UUIDs include that operation; uncertain responses/reloads retain it, and confirmed success clears it so the next visit gets fresh identities. Submission waits for storage initialization, and unavailable storage explains recovery instead of risking duplicate retry IDs. Session closure or manually cleared storage is outside the tab's retry lifetime.

Full382 tests/build and45 project/task/starter browser cases pass, including uncertain reload, successful rotation and unavailable storage. Property tests cover stable receipt identities, project isolation and operation rotation. Evidence: `/tmp/starter-operation-verify.log`, `/tmp/starter-operation-fixture-build.log`, `/tmp/starter-operation-e2e.log`. SQL022 and the assembled deliberate re-add workflow still require the connected schema dependency.

PR51 `1d45055` now checks option choice state before acknowledging a creation retry. Full397 tests/build and21 Home lifecycle browser cases pass; finding3939819811 answered in3939848492, rereview5550236343 requested. Home editor pristine refresh remains independent follow-up work.

PR45 review returned four findings. Missing routine019 (3939825778) is acknowledged and remains an integration gate. Saved-meal version checks, stable library/default-grocery creation IDs and leftover/source date serialization are being fixed locally for3939825782/85/88. The new migration was generated with Supabase CLI; local DB tests still fail to connect. No source-date race proof is claimed from sequential tests or an unavailable database.

## External meal merge and follow-up delivery

GitHub records PR45 as externally squash-merged at 05:40:48 UTC, yielding main12a4b77 from f95b190. Both commits have the identical tree574929fac2597416959b8f86a2e9aeeceece397e. Task tools did not merge the PR. Auto-review narrowly approved reparenting only the already-verified meal follow-up onto that identical merged tree, preserving its exact content; this does not authorize blocked dependency integration. The separate follow-up is PR59, initially a854e40, now61984a3 after local test-connection corrections. The original PR45's old review findings are answered with the follow-up link; missing routine019 stays open.

PR59 adds authoritative meal edit versions, stable new library/default-grocery identities and serialized leftover/source dates. Full369 tests/build and39 meal browser cases pass. Hosted SQL032 passes. SQL033 initially stopped at connection setup: the disposable CLI database trusts socket and loopback TCP connections, and dblink requires actual password authentication for a non-superuser even when a password is supplied. The latest test uses the existing restricted dblink_connect_u function without changing application grants. Its execution remains pending. Codex3939892988/93 concern successful-save lifetimes; exact server-redirect regressions are being run before accepting those findings.

PR47 at2ecbbf1 and PR52 at0a93d51 received explicit positive Codex reviews5550224401 and5550297924. PR47's three corrected threads are resolved. PR51 at4063d64 fixes archived decision choices: 398 tests/type/lint checks, production build and12 browser cases pass; SQL034 and fresh review are pending. The original restricted build failed reading TypeScript configuration, while the same production fixture build outside the sandbox passed. Home pristine-editor and attachment refresh behavior remains active work.

## Home editor and meal redirect verification

PR51 e0e2842 accepts pristine partner updates and keeps dirty values, selection labels, new IDs and upload state intact. Full398 tests/build and27 browser cases pass, including one pending upload surviving refresh with only one request. Rereview5550400371 requested. The archived-decision predecessor4063d64 has positive review5550386931, but hosted SQL034 never starts because migration setup fails on the known missing attachment helper. Local host execution separately confirms ECONNREFUSED.

PR59 34809bf adds exact successful-save regressions. All six cases pass across three browser profiles with real same-URL server redirects and no cookie changes: consecutive meal edits get current versions, and consecutive default-grocery creations rotate IDs. Both findings are disputed with this evidence; rereview5550394806 requested. Full369 tests/build pass. SQL033 is still blocked at connection setup: restricted dblink_connect_u is not granted to the CLI role. Official Supabase HBA config confirms loopback trust versus container-network SCRAM, so the next test correction uses this project's disposable Docker service name with ordinary dblink and local credentials, without changing grants. Default-grocery descriptive edit versions are a separate confirmed gap now being implemented.

The three corrected PR52 starter/auth threads3938831101,3938831110,3939832208 are resolved following positive review. Existing integration findings remain open.
