import { Text, View } from "react-native";

export default function PlanScreen() {
  return (
    <View
      style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
      testID="plan-screen"
    >
      <Text>Plan — lands in the Plan PR.</Text>
    </View>
  );
}
